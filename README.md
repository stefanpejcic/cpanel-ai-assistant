# cpanel-plugin-boilerplate


## install

```bash
cd /root && git clone https://github.com/stefanpejcic/cpanel-plugin-boilerplate && \
  cd cpanel-plugin-boilerplate && bash INSTALL.sh && \
  rm -rf /root/cpanel-plugin-boilerplate
```
